{
  "name": "rosary",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A mobile-first, installable Rosary prayer guide.",
  "scripts": {
    "dev": "npm run build && node scripts/serve.mjs --host 0.0.0.0 --port 4173",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve.mjs --host 0.0.0.0 --port 4173",
    "typecheck": "tsc --noEmit",
    "test:unit": "npm run build --silent && node --test tests/unit/*.test.mjs tests/integration/*.test.mjs",
    "test": "npm run test:unit",
    "validate:repo": "node scripts/validate-repository.mjs",
    "check": "npm run typecheck && npm run test:unit && npm run validate:repo"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.11.3",
    "@playwright/test": "1.62.0",
    "typescript": "5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
