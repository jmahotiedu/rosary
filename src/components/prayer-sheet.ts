import type { PrayerStep, MysterySetId } from "../domain/prayer-step";
import { PRAYERS } from "../data/prayers"; import { MYSTERIES } from "../data/mysteries";
export function renderPrayerSheet(step:PrayerStep,set:MysterySetId):string { const mystery=step.mysteryIndex===undefined?null:MYSTERIES[set][step.mysteryIndex]; return `<section class="prayer-sheet" aria-live="polite"><div class="sheet-handle" aria-hidden="true"></div><p class="step-location">${step.location}</p><h2>${step.label}</h2>${mystery?`<aside class="mystery-card"><p>${set} mystery ${step.mysteryIndex!+1}</p><h3>${mystery.name}</h3><span>${mystery.meditation}</span></aside>`:""}<div class="prayer-list">${step.prayerIds.map(id=>{const p=PRAYERS[id];return `<article class="prayer"><div class="prayer-heading"><h3>${p.title}</h3><span>Say once</span></div><p>${p.text.replaceAll("\n\n", "</p><p>")}</p></article>`}).join("")}</div><div class="sheet-actions"><button data-action="previous">Previous</button><button class="primary" data-action="next">Next bead</button></div></section>`; }
