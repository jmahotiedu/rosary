import { access,readFile } from "node:fs/promises";const required=["dist/index.html","dist/main.js","dist/manifest.webmanifest","dist/sw.js","README.md","LICENSE"];for(const p of required)await access(p);const html=await readFile("dist/index.html","utf8");if(!html.includes("manifest.webmanifest"))throw new Error("manifest link missing");console.log("Repository validation passed");